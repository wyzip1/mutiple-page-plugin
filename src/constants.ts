/**
 * Valid entry file patterns
 */
export const ENTRY_PATTERNS = ['main.ts', 'main.tsx'];

/**
 * Default HTML template variables
 */
export const HTML_DEFAULTS = {
  LANG: 'en',
  VIEWPORT: 'width=device-width, initial-scale=1.0, user-scalable=no',
  ROOT_ID: 'app'
} as const;